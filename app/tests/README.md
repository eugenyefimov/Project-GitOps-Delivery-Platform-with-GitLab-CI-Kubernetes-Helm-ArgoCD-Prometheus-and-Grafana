# Test Structure

- `unit/`: fast tests for application behavior and endpoint contracts.
- `integration/`: broader tests that validate runtime integrations and HTTP surface.

This structure is intentionally small at bootstrap stage and can grow with features.
