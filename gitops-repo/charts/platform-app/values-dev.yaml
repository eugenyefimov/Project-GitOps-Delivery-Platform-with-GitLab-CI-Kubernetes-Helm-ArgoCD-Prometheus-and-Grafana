replicaCount: 1

image:
  tag: "dev-latest"
  digest: ""

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "250m"
    memory: "256Mi"

config:
  data:
    APP_ENV: dev

env:
  APP_LOG_LEVEL: debug

autoscaling:
  enabled: false
